<GenericList MyList="measurementUnits">
    <Body>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th style="width:168px">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var measurementUnit in measurementUnits!)
                {
                    <tr>
                        <td>@measurementUnit.Name</td>
                        <td>
                            <HxButton Color="ThemeColor.Secondary" data-bs-dismiss="modal" Text="Seleccionar" OnClick="@(() => OnSelectedMeasurementUnitChanged.InvokeAsync($"{measurementUnit.Id},{measurementUnit.Name}"))" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </Body>
</GenericList>