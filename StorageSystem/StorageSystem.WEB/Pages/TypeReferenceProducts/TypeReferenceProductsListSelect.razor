<GenericList MyList="typeReferenceProducts">
    <Body>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Nombre</th>
                    <th style="width:168px">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var typeReferenceProduct in typeReferenceProducts!)
                {
                    <tr>
                        <td>@typeReferenceProduct.Name</td>
                        <td>
                            <button type="button" class="btn btn-secondary" @onclick="@(() => OnSelectedTypeReferenceProductChanged.InvokeAsync($"{typeReferenceProduct.Id}"))" data-bs-dismiss="modal">Seleccionar</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </Body>
</GenericList>

